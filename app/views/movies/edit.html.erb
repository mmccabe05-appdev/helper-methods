<h1>Edit movie</h1>

<% @movie.errors.full_messages.each do |message| %>
  <p style="color: red;"><%= message %></p>
<% end %>


<%= form_with(url: movie_path(@movie.id), method: :patch) do %>

  <div>
    <%= label_tag :title %>
    <%= text_field_tag :title, @movie.title, id: "title" %>
  </div>

  <div>
    <label for="description">
      Description
    </label>
    <%= label_tag :description, "Description" %>
    <%= text_area_tag :description, @movie.description, id: "description", rows: "5" %>

  </div>
  <%= button_tag "Update movie" %>

<% end %>

<!-- removing from here down -->
